check what do first : delete for DB or update user INFO | at the same time ? 
add owner to models as a user owner ! 
backend - making order 
back btn 
onDrop save user info 
drop in empty container 
task click --> edit & add list 
img --- > board & profile --> if image in not available set the default ! 
deploy the app 
upperCase titles ? 

